#include<bits/stdc++.h>
using namespace std;
int main() {
    // Array declaration
    vector<int> x;

    // Array declaration for a given size
    int size = 10;
    vector<int> y(size);

    // Array declaration with initial  value
    int initialValue = 5;
    vector<int> z(size, initialValue);

    return 0;
}
